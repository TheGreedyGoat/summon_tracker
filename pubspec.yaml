name: summon_tracker
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.12.2

dependencies:
  build_runner: ^2.15.1
  flutter:
    sdk: flutter
  freezed: ^3.2.5
  freezed_annotation: ^3.1.0
  math_parser: ^1.5.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
